import BaseRepository from "@/backend/repositories/base-repository";

class ColumnRepository implements BaseRepository{
    getAll = () => {
        return [];
    }
}

export default ColumnRepository